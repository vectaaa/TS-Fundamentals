let phone = 234_812_232_121;
let course = 'Maths';
let is_Finished = true;
let level;

let number = [1,2,4];

//Tuples
let user: [number, string] = [1, 'Ready'];
console.log(user);

//Enums they are used to specify constants in classifications e.g {Sizes of clothes, days of the week, colors, month of the year etc.}
enum Sizes {Small, Medium, Large};
let mySize = Sizes.Small;
console.log(mySize);